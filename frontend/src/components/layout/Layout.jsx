// DEPRECATED — kept only so that accidental imports do not break the build.
// The real layout lives in ./AppLayout.jsx (sidebar + topbar + routed outlet,
// using the tokens.css design system that was ported verbatim from
// "MWT ONE.html"). This file used to contain a parallel Tailwind-based layout
// from an earlier iteration; it was removed to avoid two sources of truth and
// to drop the lucide-react dependency.
//
// If you are looking for the layout, edit AppLayout.jsx.
export { default } from "./AppLayout.jsx";
